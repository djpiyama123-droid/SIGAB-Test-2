/**
 * sigah.js — Proxy de re-exportación para compatibilidad
 * Re-exporta el cliente centralizado API de sigab.js para satisfacer las
 * importaciones de '../api/sigah' a lo largo del frontend.
 */
export * from './sigab';
