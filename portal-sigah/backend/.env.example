# Backend SIGAH — copiar a backend/.env y completar en el VPS
NODE_ENV=production
PORT=8000

# OBLIGATORIO en producción — generar con: openssl rand -hex 32
JWT_SECRET=

# Dominios permitidos para CORS (separados por coma)
CORS_ORIGINS=https://sigab.129-121-100-147.sslip.io

# Identificador del equipo (aparece en el panel Cerebro)
EQUIPO_NOMBRE=VPS-Bluehost

# Contraseñas individuales (sobrescriben los defaults de dev)
PWD_GUSTAVO=
PWD_CARLOS=
PWD_DEMO=
