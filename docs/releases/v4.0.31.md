# v4.0.31 — 2026-07-09 · Correcciones menores post-revisión

## Cambios
1. **Migración `a1b2c3d4e5f6` (multi-tenancy fase 1)** — `COMPOSITE_INDEXES` referenciaba 2 columnas inexistentes:
   - `preventivos_programados.fecha_programada` → **`proxima_ejecucion`** (columna real, verificada con `DESCRIBE` en producción y en `models/preventivo.py`).
   - `tecnovigilancia_eventos.fecha` → **`fecha_evento`** (columna real usada en `routes/tecnovigilancia.py`).
   Impacto: en producción NINGUNO (la migración se aplicó a mano y los índices reales ya existen); en una **instalación limpia** (hospital #2 / entorno dev) el `upgrade()` reventaba al crear los índices compuestos.
2. **`PreventivoForm.jsx`** — cleanup del `useEffect` que carga el equipo: una promesa tardía ya no puede sobreescribir el estado con el equipo equivocado (hallazgo real de la revisión adversarial de hoy).

## ⚠️ Operación pendiente (VPS, manual)
Verificado 2026-07-09 por Hermes: la tabla **`alembic_version` NO existe en la BD de producción** — las migraciones se aplicaron a mano y alembic no rastrea el estado. Antes de que cualquier flujo intente `alembic upgrade` en producción:
```bash
# dentro del contenedor del backend, tras verificar que el esquema está al día:
alembic stamp head
```
NUNCA correr `alembic upgrade` en producción sin este `stamp` previo — intentaría re-aplicar todo desde cero.

## Rollback
`git revert` del commit sobre `v4.0/piloto-clinica-1`.
